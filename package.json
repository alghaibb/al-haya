{
  "name": "al-haya",
  "version": "1.0.0",
  "description": "An islamic e-commerce shop for men, women and kids.",
  "main": "server/server.ts",
  "scripts": {
    "start": "concurrently \"yarn run start-server\" \"yarn run start-client\"",
    "start-server": "cd server && yarn start",
    "start-client": "cd client && yarn dev",
    "install-all": "yarn && cd server && yarn && cd ../client && yarn"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
